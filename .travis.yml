language: python
python: "3.6"

# command to install dependencies
install:
  - pip install -r requirements.txt --ignore-installed

script:
  - pytest

after_success:
  - coverage run -m pytest
  - coverage report -m